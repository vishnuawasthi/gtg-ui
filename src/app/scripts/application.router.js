/**
 * Created by Dell on 1/14/2017.
 */

/*angular.module('shoppingApp').config(function ($routeProvider) {
    $routeProvider.when("/", {
            templateUrl: "src/templates/login.success.templates.html",
            controller: LoginController,
            controllerAs: 'vm',
            resolve: function () {
                if ($roootScope.isLogggedIn) {
                    $location.path('home');
                }

            }
        }
    );


});*/
